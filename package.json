{
  "name": "linshe-wechat-miniprogram",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "node scripts/check-project.js",
    "test": "node --test",
    "ci": "npm run check && npm test"
  }
}

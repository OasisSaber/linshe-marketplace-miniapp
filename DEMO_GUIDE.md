# 邻舍小程序 Demo 演示说明

## 项目定位

本项目是移动应用开发期末作业 Demo，展示“邻舍”校园二手交易微信小程序的主干功能结构。项目保留学生认证、发布、出价、结算、面交核销、消息、社区和个人中心等完整流程，但支付与核销均为本地模拟，不涉及真实金钱交易。

## 仓库打开方式

1. 克隆或下载本仓库。
2. 使用微信开发者工具导入仓库根目录。
3. 保持 `project.config.json` 中的 `touristappid`，或按本地开发环境配置测试 AppID；请勿提交个人 AppID。
4. 编译模式选择普通编译，入口页为 `pages/index/index`。
5. 在仓库根目录执行 `npm run check` 做本地校验。

## 推荐演示路径

1. 首页浏览：进入首页，查看校园定位、搜索、分类、活动横幅和附近好物。
2. 分类筛选：点击教材课本、电子数码、宿舍神器、运动装备、时尚穿搭、乐器音响等分类，查看不同商品列表。
3. 商品详情：点击任意商品卡片进入详情页，查看价格、成色、卖家信息、商品描述和面交地点。
4. 学生认证：未认证时点击购买会跳转到学生认证页；勾选协议，输入包含 `.edu` 的邮箱和验证码后完成认证。
5. 出价结算：从详情页进入出价页，发送出价后进入确认订单页。
6. Demo 支付：结算页点击确认支付，系统自动创建订单并模拟支付通过，订单状态进入 `wait_meetup`。
7. Demo 核销：订单详情页点击确认收货，系统自动模拟核销通过，订单状态进入 `completed`。
8. 消息会话：进入消息 Tab，点击会话进入聊天详情，可发送本地消息，也可从聊天页进入出价或购买。
9. 发布与社区：进入发布 Tab 演示认证阻断或已认证发布；进入圈子 Tab 点击发帖，会生成一条本地 Demo 动态。
10. 个人中心：查看认证状态、钱包、收藏、安全中心等入口，体验本地模拟交互。

## 关键文件

- `app.json`：17 个页面路由和 5 个 TabBar 入口。
- `project.config.json`：微信开发者工具项目配置。
- `data/items.js`：19 个多分类 Demo 商品数据。
- `services/auth.js`：学生认证与权限阻断。
- `services/orders.js`：Demo 订单状态机，`wait_pay -> wait_meetup -> completed`。
- `services/items.js`：商品列表、搜索、发布和收藏映射。
- `services/chats.js`：会话列表与本地消息追加。
- `scripts/check-project.js`：项目结构、页面文件、数据模型和主流程校验。

## Demo 边界

- 不接入真实微信支付。
- 不产生真实资金流动。
- 不接入真实扫码收款。
- 图片上传、云数据库、实时 IM 和订阅消息属于后续正式化方向。

## Debug 记录

- 2026-06-26：修复学生认证登录和认证两阶段的版式问题，去除认证页重复导航，压缩首屏品牌区和按钮区间距，避免按钮文字与协议文字错位。
- 2026-06-26：修复商品详情样例内容错乱，商品图片、商品描述、卖家信息和面交地点统一由 `item_id` 派生，确保示例商品不会互相串项。
- 2026-06-27：完成代码质量整改，启用组件按需注入，补齐 5 个 TabBar 本地图标，移出非运行大文件，确保小程序根目录无超过 200KB 的图片和音频资源。
- 2026-06-27：完成 UI 布局 Debug，统一按钮、头像、图标等固定尺寸组件的 Flex 居中规则，修复登录、钱包、消息/聊天、我的页认证徽章等文字和组件偏移问题。
- 2026-06-27：完成重复导航清理，订单确认、订单核销、收藏、钱包、安全、发布、筛选、卖家主页和发布成功等页面改用原生导航，保留聊天页自定义导航。
- 2026-07-04：补齐首页附近好物第 4 个 Demo 商品，首页两列推荐卡片不再出现最后一行单卡缺项；商品封面改为读取 `item.images[0]`。
- 2026-07-04：补齐分类筛选、首页收藏、活动入口、详情收藏/分享/聊天、聊天工具、圈子点赞/关注/分享、钱包提现/充值、订单联系卖家和个人中心空入口等本地 Demo 交互。
- 2026-07-04：新增 15 个多分类 Demo 商品，覆盖教材课本、电子数码、宿舍神器、运动装备、时尚穿搭、乐器音响；每个分类至少 3 个商品，详情页新增通用描述生成规则。
- 2026-07-05：整理正式提交材料，生成 Word 与 PDF 两份开发文档，并清理临时输出、版本控制目录、开发过程记录和无关依赖目录。

## 校验结论

当前源码可执行 `npm run check` 完成自动校验。校验内容包括页面四件套、路由注册、TabBar 图标、事件绑定、商品分类覆盖、订单状态流转、学生认证、聊天追加、发布商品和主流程 smoke test。最近一次校验结果为：`Lin She mini program check passed.`

# 邻舍小程序 Demo 演示说明

## 项目定位

本项目是移动应用开发课程期末作业 Demo，用于展示“邻舍”校园二手交易微信小程序的主要页面、交互和本地数据路径。

当前版本不接入后端、云数据库、实时通信或真实支付，不产生真实资金流动，也不构成可直接上线的校园交易服务。当前能力与 Backlog 以 [`README.md`](./README.md) 为准。

## 仓库打开方式

1. 克隆或下载本仓库。
2. 使用微信开发者工具导入仓库根目录。
3. 保持 `project.config.json` 中的 `touristappid`，或只在本地个人配置中设置测试 AppID。
4. 编译模式选择“普通编译”。
5. 入口页为 `pages/index/index`。
6. 可在仓库根目录执行 `npm run check` 进行结构检查和正常路径 Smoke Test。

## 推荐演示路径

### 1. 首页浏览

进入首页，展示：

- 校园名称
- 标题和地点关键词过滤
- 商品分类切换
- 活动入口
- 附近好物

首页商品流和分类切换使用共享本地商品数据。

### 2. 商品详情

点击正常商品卡片进入详情页，展示价格、成色、卖家摘要、商品描述和面交地点。

已知边界：无效 `item_id`、`order_id` 和 `chat_id` 当前可能回退到第一条数据，修复由 [#1](../../issues/1) 跟踪。演示时应从页面正常入口进入，不手工构造错误参数。

### 3. 学生认证阻断

未认证时，从商品交易或发布入口进入学生认证页：

1. 勾选用户协议和隐私政策。
2. 进入校园邮箱认证界面。
3. 输入包含 `.edu` 的演示邮箱。
4. 输入验证码字段并提交。

当前认证仅为本地流程演示：

- 不会真实发送验证码。
- 不会验证真实学生身份。
- 当前验证码字段尚未参与服务层验证。
- 更严格、可测试的 Demo 验证规则由 [#8](../../issues/8) 跟踪。

### 4. 出价与结算

从商品详情进入出价页，修改演示出价后进入结算页。

结算页会展示商品金额、平台担保费、校园面交优惠和实付金额。当前成交金额主要用于页面展示，完整订单快照和商品状态同步由 [#3](../../issues/3) 跟踪。

### 5. Demo 支付

在结算页点击“确认支付”，系统会在运行时内存中创建订单并进入正常演示状态：

```text
wait_pay → wait_meetup
```

当前实现是正常路径状态流转演示，尚未严格拒绝所有非法状态跳转。订单状态约束由 [#2](../../issues/2) 跟踪。

### 6. Demo 核销

在订单详情页点击“确认收货”，系统会将正常演示订单推进为：

```text
wait_meetup → completed
```

该操作不扫描真实二维码，也不产生资金结算。

### 7. 消息会话

进入消息 Tab，点击已有会话进入聊天详情。

可演示：

- 查看本地历史消息
- 追加当前运行时消息
- 从聊天页进入出价或购买
- 图片、位置、电话等工具入口提示

消息不会发送到其他设备，重启小程序后新增消息会重置。

### 8. 发布商品

进入发布 Tab，可展示未认证阻断和已认证后的发布页面。

当前部分字段仍为界面演示：

- 标题、价格和成色可进入本地商品创建流程。
- 分类、描述、图片、地点和面交开关尚未全部写入商品模型。
- 发布成功页当前使用固定示例商品。

完整发布闭环由 [#4](../../issues/4) 跟踪。

### 9. 收藏与筛选

首页和详情页可展示收藏按钮，筛选页可展示排序和筛选界面。

已知边界：

- 收藏状态尚未在首页、详情、收藏页和个人中心之间统一，由 [#5](../../issues/5) 跟踪。
- 独立筛选页当前使用静态示例结果，真实数据筛选由 [#6](../../issues/6) 跟踪。

### 10. 社区与个人中心

进入圈子 Tab，可展示本地发帖、点赞、关注和分享交互。

进入个人中心，可展示认证状态、钱包、安全页面和订单入口。钱包充值、提现、统计和部分菜单为 UI 或本地 Toast 演示，不代表真实账户或资金功能。

卖家主页当前使用固定示例数据，按 `seller_id` 加载由 [#7](../../issues/7) 跟踪。

## Demo 边界

当前明确不实现：

- 真实微信支付、退款和资金托管
- 真实扫码收款或面交核销
- 后端服务和生产数据库
- 实时 IM 和订阅消息
- 真实图片上传和 GPS 定位
- 多用户并发、权限隔离和交易审计
- 内容审核、纠纷处理和正式运营能力

这些能力不属于当前课程 Demo 的发布阻断项。只有项目准备成为真实公共服务时，才需要建立对应的身份、权限、安全和运维体系。

## 项目检查

执行：

```bash
npm run check
```

当前命令检查：

- 项目必需文件和 JSON 格式
- 页面四件套和路由注册
- TabBar 图标与页面标题
- 部分事件处理器和静态跳转
- 商品、订单字段结构和样例分类覆盖
- 正常路径下的认证、订单流转、消息追加和商品创建 Smoke Test

成功时输出：

```text
Lin She mini program check passed.
```

该检查不是完整测试套件，不证明所有异常路径、跨页面状态或 UI 行为均已正确。失败路径测试和 GitHub Actions 由 [#9](../../issues/9) 跟踪。

## 文档来源

- 当前能力、限制和 Backlog：以根目录 [`README.md`](./README.md) 为准。
- 本文件：只用于演示操作和已知演示边界。
- `配套开发文档/` 中的 DOCX/PDF：课程提交快照，不保证随源码持续更新。
